name: Deploy to Production

on:
  push:
    branches:
      - production

jobs:
  deploy:
    name: Deploy to Firebase
    runs-on: ubuntu-latest
    environment: production
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      
      - name: Install root dependencies
        run: npm ci
      
      - name: Install functions dependencies
        run: |
          cd functions
          npm ci
          cd ..
      
      # Create functions/.env from GitHub secrets
      - name: Create functions .env file
        run: |
          echo "Creating functions/.env from GitHub secrets..."
          cd functions
          echo "# Auto-generated from GitHub Secrets" > .env
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" >> .env
          echo "✅ Environment file created"
      
      - name: Build functions
        run: |
          cd functions
          npm run build
          cd ..
        env:
          NODE_ENV: production
      
      - name: Setup Firebase CLI
        run: npm install -g firebase-tools
      
      - name: Authenticate with Firebase
        run: |
          if [ -z "$FIREBASE_TOKEN" ]; then
            echo "❌ FIREBASE_TOKEN secret is not set. Please add it to GitHub Secrets."
            echo "To generate a token, run: firebase login:ci"
            exit 1
          fi
          echo "✅ Firebase token is set (length: ${#FIREBASE_TOKEN})"
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
      
      - name: Deploy Firebase Functions (Production)
        run: firebase deploy --only functions --token "$FIREBASE_TOKEN"
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          NODE_ENV: production
      
      - name: Enable Firebase Web Frameworks experiment
        run: |
          firebase use choicestory-b3135 --token "$FIREBASE_TOKEN"
          firebase experiments:enable webframeworks --token "$FIREBASE_TOKEN" || echo "⚠️  Experiment may already be enabled"
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
      
      # Create root .env for Next.js build (if needed by hosting)
      - name: Create root .env for Next.js
        run: |
          echo "Creating root .env for Next.js build..."
          if [ -n "${{ env.NEXT_PUBLIC_FIREBASE_API_KEY }}" ]; then
            echo "NEXT_PUBLIC_FIREBASE_API_KEY=${{ env.NEXT_PUBLIC_FIREBASE_API_KEY }}" > .env.production
            echo "NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${{ env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}" >> .env.production
            echo "NEXT_PUBLIC_FIREBASE_PROJECT_ID=${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}" >> .env.production
            echo "NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}" >> .env.production
            echo "NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}" >> .env.production
            echo "NEXT_PUBLIC_FIREBASE_APP_ID=${{ env.NEXT_PUBLIC_FIREBASE_APP_ID }}" >> .env.production
            echo "NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=${{ secrets.NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID }}" >> .env.production
            echo "NEXT_PUBLIC_FIREBASE_ENV=production" >> .env.production
            echo "✅ Root environment file created"
          else
            echo "⚠️  No Firebase config secrets found, skipping .env.production creation"
          fi
      
      # Clean previous build to ensure fresh build
      - name: Clean previous build artifacts
        run: |
          echo "🧹 Cleaning previous build artifacts..."
          rm -rf .next
          echo "✅ Clean complete"
      
      # Build Next.js app for production
      - name: Build Next.js app for production
        run: |
          echo "🔨 Building Next.js app for production..."
          npm run build
        env:
          NODE_ENV: production
          NEXT_PUBLIC_FIREBASE_ENV: production
          FIREBASE_ENV: production
      
      - name: Deploy to Production Hosting
        run: |
          echo "🔧 Setting Firebase project..."
          firebase use choicestory-b3135 --token "$FIREBASE_TOKEN"
          echo "🔧 Applying production hosting target mapping..."
          firebase target:apply hosting production choicestory-b3135 --token "$FIREBASE_TOKEN" || echo "⚠️  Target may already be configured"
          echo "📦 Deploying to production hosting (site: choicestory-b3135)..."
          firebase deploy --only hosting:production --token "$FIREBASE_TOKEN"
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
          NODE_ENV: production
          NEXT_PUBLIC_FIREBASE_ENV: production

